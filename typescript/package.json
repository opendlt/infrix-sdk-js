{
  "name": "@infrix/client",
  "version": "0.1.0",
  "description": "TypeScript client for the Infrix smart contract platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "tsc --noEmit && node --test dist/index.test.js"
  },
  "keywords": ["infrix", "smart-contracts", "wasm", "accumulate", "blockchain"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
