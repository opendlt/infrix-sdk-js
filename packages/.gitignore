# Generated at build time by each package's scripts/vendor.mjs (single source of
# truth is pkg/nexus/web). Regenerated by `npm run build`; never hand-edited.
*/vendor/

# Installed dependencies (lockfile is committed; node_modules is not).
node_modules/
*/node_modules/
